# Tapandrate — Admin Guide

A one-page guide to running your link profiles. Everything happens in the admin dashboard; your customers only ever see the public profile pages.

## Signing in

1. Go to your admin URL and add `/login` (e.g. `https://yourdomain.co.uk/login`).
2. Enter the email and password you were given.
3. You'll land on the dashboard. Sessions stay signed in across refreshes and sign you out automatically after 60 minutes of inactivity.
4. **Sign out** any time with the button in the top-right of the dashboard.

There is no public sign-up — accounts are created by the administrator, so only people with a login can reach the dashboard.

## Creating a profile

1. In the purple **"Create a new profile"** box, type a name (this is just a label for you, e.g. "Juices4Life — Wembley").
2. Click **Create profile**. You're taken straight into the editor.

## Editing a profile

Inside the editor you can set:

- **Header image** — the wide image at the top (the background).
- **Logo / secondary image** — the round image that sits in front of the header.
- **Business name** and **tagline**.
- **Background colour** and **button colour** (use the colour picker or eyedropper).
- **Main button** — its label (e.g. "View Menu") and the link it opens.
- **Links** — add as many as you need. Pick an icon, give each a title and URL, and reorder or delete them.

Click **Save** (or **Save Design**) when done. Use **Preview** to open the live public page in a new tab exactly as customers will see it.

## Sharing a profile

Each profile has its own public page and QR code.

- **Copy URL** (the copy icon on a profile card) puts the public link on your clipboard.
- **QR code** — the square code on each card points to that profile; download or screenshot it to print.
- **Open public URL** (under the **⋯** menu) opens the live page.

Share the link or QR with customers — no login needed to view it.

## Organising with folders

- Create a folder in the **New folder** box on the left: type a name, pick a colour, and click **+**.
- **Rename** a folder by hovering it and clicking the pencil icon.
- **Delete** a folder with the ✕ (profiles inside move to Uncategorised).
- Move a profile into a folder via the **⋯** menu on its card.
- Click any folder to filter the list to just those profiles. Use the search box to find a profile by name.

## View counts

Each profile card shows how many times its public page has been viewed (next to "Updated"). The count updates as customers open the page — **refresh the dashboard** to see the latest numbers. Counts are shared across every device, and obvious bots/repeat refreshes are filtered out so the numbers stay honest.

## Duplicating, pausing, and deleting

- **Duplicate** (the copy-plus icon) makes a full copy of a profile to tweak.
- **Select** one or more profiles (checkboxes) to **Pause** them (takes the public page offline — visitors see "This page is no longer available") or **Delete** them in bulk.
- Delete a single profile from the **⋯** menu. Deletes are permanent and ask for confirmation first.

## Need help?

Contact your developer during the support window for any bugs or changes.
